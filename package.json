{
  "dependencies": {
    "mockingoose": "^2.16.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "@vitest/ui": "^2.1.8",
    "mongodb-memory-server": "^10.1.3",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "vitest": {
    "globals": true,
    "environment": "node",
    "testTimeout": 10000,
    "coverage": {
      "enabled": true,
      "provider": "c8",
      "reporter": [
        "text",
        "html"
      ]
    }
  }
}
